= title("Latest Questions")
- collection.each do |question|
  .row
    .col-lg-12
      .well
        %h3= link_to question.title, question
        %p= "Posted #{time_ago_in_words(question.created_at)} ago"